server:
  port: 8075
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rest-spring
    username: root
    password: Julio@1995!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false